---
id: 3-004
phase: 3
title: v0.1 milestone close — ROADMAP, CHANGELOG, REQUIREMENTS updates
depends_on: [3-001, 3-002, 3-003]
gap_closure: false
language: Python
---

## Objective

Close the v0.1 milestone by updating three documentation files to reflect that
Phase 3 is complete and FORGE v0.1 is declared.

This plan item is the final gate for Phase 3. It must only be executed after
3-001, 3-002, and 3-003 are implemented and all their pytest verification gates
pass.

## Acceptance Criteria

1. `.planning/REQUIREMENTS.md` — Phase 3 section updated:
   - Status line reads `**Status:** Verified <date>. All Phase 3 ACs pass.`
   - Exit gate bullet marked complete.
   - Delivered items listed (LiveToolExecutor, degraded-mode tests, log
     completeness audit, fixture parametrization).

2. `.planning/ROADMAP.md` — Phase 3 row updated:
   - Status column changes from `Planned` to `VERIFIED <date>`.
   - Phase 3 Milestones section added with all items checked.
   - Phase 2 status updated to `VERIFIED 2026-03-18` if not already.

3. `CHANGELOG.md` — v0.1 section added:
   - New `## [0.1.0] — <date>` section above `## [Unreleased]`.
   - Lists all Phase 3 deliverables under `### Added`.
   - Lists Phase 2 deliverables under `### Added` (pipeline, librarian, acceptance
     tests, fixtures) — they were previously only in `[Unreleased]`.
   - Format follows Keep a Changelog convention already established in the file.

4. `.planning/STATE.md` — Phase 3 section appended:
   - Plans executed: 3-001, 3-002, 3-003, 3-004.
   - Gates: ruff PASS, yamllint PASS, pytest N/N PASS.
   - `Next: /gsd:plan-phase 4` line added.

5. `pytest --tb=short -q` passes with 0 failures after all documentation updates
   (documentation changes must not break any test by accidentally corrupting
   Python files or YAML files).

6. `yamllint .` passes after all changes (YAML files not affected, but verify
   no accidental YAML breakage).

## Design

This plan item involves only documentation edits to existing files. No Python
code is created or modified. No new files other than documentation updates.

### REQUIREMENTS.md edit

Locate the Phase 3 section (currently starts at line 96 of REQUIREMENTS.md).
Change:

```markdown
## Phase 3 — v0.1 Acceptance Gate (Planned)

**Entry gate:** Phase 2 complete; all R2-0x tests pass.
**Exit gate:** v0.1 acceptance test runs against live GMSH + CalculiX (not mocked);
all 10 ACs pass; v0.1 milestone declared complete.

Scope: Live solver integration, degraded-mode path, log completeness audit.
```

To:

```markdown
## Phase 3 — v0.1 Acceptance Gate (COMPLETE)

**Status:** Verified <date>. All Phase 3 ACs pass. v0.1 milestone declared.
**Entry gate:** Phase 2 complete; all R2-0x tests pass. (DONE)
**Exit gate:** v0.1 acceptance test runs with LiveToolExecutor (real code paths,
degraded mode); all 10 ACs pass; v0.1 milestone declared complete. (DONE)

**Delivered:**
- `forge_agent/core/live_tool_executor.py` — LiveToolExecutor with full MCP
  Wrapper Envelope v1 output; returns `error_envelope()` on absent tools
- `forge_agent/tests/test_live_tool_executor.py` — 7 unit tests for LiveToolExecutor
- `forge_agent/tests/test_v01_acceptance_live.py` — 8 degraded-mode AC tests
- `forge_agent/tests/test_log_completeness.py` — JSONL event schema audit
- `forge_agent/tests/test_fixture_schemas.py` — parametrized fixture regression tests
```

### ROADMAP.md edit

Add Phase 3 Milestones section and update status column. The table row:
```
| 3 | v0.1 Acceptance Gate (live solvers) | Planned | TBD |
```
becomes:
```
| 3 | v0.1 Acceptance Gate (live solvers) | VERIFIED <date> | claude/briefcase-phase-implementation-gK4j5 |
```

Add section:
```markdown
## Phase 3 Milestones

- [x] LiveToolExecutor (forge_agent/core/live_tool_executor.py)
- [x] Degraded-mode acceptance tests (test_v01_acceptance_live.py)
- [x] Log completeness audit (test_log_completeness.py)
- [x] Fixture parametrization (test_fixture_schemas.py)
- [x] v0.1 milestone declared
```

### CHANGELOG.md edit

Insert new section before `## [Unreleased]`:

```markdown
## [0.1.0] — <date>

### Added (Phase 3 — v0.1 Acceptance Gate)
- `LiveToolExecutor` — exercises real Python code paths for GMSH and CalculiX;
  returns full MCP Wrapper Envelope v1 on success and `error_envelope()` on
  absent tools (degraded mode)
- Degraded-mode acceptance test suite (`test_v01_acceptance_live.py`) — proves
  all 10 v0.1 ACs hold when tools are absent
- JSONL log completeness audit (`test_log_completeness.py`) — verifies every
  JSONL event schema: `event`, `phase`, `trace_id`, `timestamp`, `duration_ms`
- Fixture parametrization (`test_fixture_schemas.py`) — drives phase_1_2,
  phase_1_3, and golden/verifier_red_team fixtures through pytest

### Added (Phase 2 — MVP Pipeline Integration)
- `forge_agent/core/pipeline.py` — 9-phase PipelineRunner with DefaultToolExecutor
  and MockToolExecutor; emits JSONL events with trace_id propagation
- `forge_agent/agents/librarian.py` — LibrarianAgent with intake, retrieve,
  amnesia_check, detect_gaps
- `forge_agent/tests/test_v01_acceptance.py` — 10 AC tests (all pass with
  MockToolExecutor)
- `forge-tests/fixtures/phase_1_2/`, `phase_1_3/`, `golden/verifier_red_team/`
  — populated YAML fixture data files
```

### STATE.md edit

Append new Phase 3 section after the existing Phase 2 section:

```markdown
## Phase 3 — Completed <date>

Plans:
- 3-001  LiveToolExecutor (forge_agent/core/live_tool_executor.py)
- 3-002  Degraded-mode acceptance tests (forge_agent/tests/test_v01_acceptance_live.py)
- 3-003  Log completeness audit + fixture parametrization
- 3-004  v0.1 milestone close (ROADMAP, CHANGELOG, REQUIREMENTS, STATE)

Gates: ruff PASS · yamllint PASS · pytest N/N PASS
v0.1 milestone declared: FORGE v0.1 complete.

Next: /gsd:plan-phase 4
```

## P-Guard Checklist

- **P1**: No method calls in documentation files. Not applicable.
- **P2**: No functions in documentation files. Not applicable.
- **P3**: No circuit breakers. Not applicable.
- **YAML**: REQUIREMENTS.md, ROADMAP.md, STATE.md, CHANGELOG.md are all
  Markdown, not YAML. No YAML indentation rules apply. However, verify that
  editing these files does not accidentally damage any adjacent `.yaml` files.
- **No `|| true`**: No CI commands in this plan item. The verification gate
  `pytest --tb=short -q` must be run bare (no `|| true`).
- **yamllint**: Run `yamllint .` after edits to confirm no YAML files were
  accidentally corrupted.

## Files to Edit

| Action | File |
|---|---|
| Edit | `.planning/REQUIREMENTS.md` |
| Edit | `.planning/ROADMAP.md` |
| Edit | `CHANGELOG.md` |
| Edit | `.planning/STATE.md` |

## Implementation Steps

1. Confirm 3-001, 3-002, 3-003 are all verified:
   ```bash
   pytest --tb=short -q
   ruff check . --ignore E501
   ```
   Both must pass before proceeding.

2. Edit `.planning/REQUIREMENTS.md`:
   - Replace Phase 3 status from `(Planned)` to `(COMPLETE)`.
   - Add `**Status:**`, delivered items list.

3. Edit `.planning/ROADMAP.md`:
   - Update Phase 3 table row status to `VERIFIED <date>`.
   - Add branch column value.
   - Add Phase 3 Milestones section with checkboxes.

4. Edit `CHANGELOG.md`:
   - Insert `## [0.1.0]` section with Phase 2 and Phase 3 deliverables.
   - Do not remove the `[Unreleased]` section.

5. Edit `.planning/STATE.md`:
   - Append Phase 3 completion block.

6. Run final verification:
   ```bash
   yamllint .
   ruff check . --ignore E501
   pytest --tb=short -q
   ```

## Verification Gates

```bash
yamllint .
ruff check . --ignore E501
pytest --tb=short -q
```

All three must exit 0. This is the final v0.1 exit gate.

## Known Risks

- **Date placeholder**: All `<date>` placeholders must be replaced with the
  actual execution date (2026-03-18) when the plan is implemented.
- **pytest count**: The `N/N PASS` in STATE.md must be filled in with the
  actual count after implementation.
- **CHANGELOG format**: `## [0.1.0] — 2026-03-18` must follow Keep a Changelog
  exactly. Verify the date format matches `## [0.0.1] — 2026-03-05` already in
  the file.
- **Language**: Documentation only. No code changes. Polyglot doctrine does not
  restrict documentation file formats (Markdown).
