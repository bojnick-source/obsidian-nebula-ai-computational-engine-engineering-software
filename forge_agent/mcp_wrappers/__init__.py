# forge_agent.mcp_wrappers — MCP server wrappers for external engineering tools.
#
# Each server wraps a tool invocation in the frozen MCP Wrapper Envelope Contract v1
# (docs/contracts/mcp-wrapper-envelope.md) and exposes it via FastMCP stdio transport.
